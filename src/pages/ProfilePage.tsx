// Route: /profile — Own profile, accessed via avatar on Home (Part 10, Part 13)
// Contains public profile preview, avatar, display name, bio, and access to Settings.
export default function ProfilePage() {
  return <div>Profile — /profile</div>
}
