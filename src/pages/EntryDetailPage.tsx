// Route: /entries/:entryId — Entry Detail, read-only (Part 10)
// Full record of one event regardless of type. Editing from source destination.
export default function EntryDetailPage() {
  return <div>Entry Detail — /entries/:entryId</div>
}
