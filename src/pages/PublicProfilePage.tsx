// Route: /builds/:username — Public Profile (Part 10, Part 13)
// The ONLY non-authenticated route. Anyone can view without logging in.
export default function PublicProfilePage() {
  return <div>Public Profile — /builds/:username</div>
}
