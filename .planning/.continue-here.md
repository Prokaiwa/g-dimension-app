---
context: default
phase: tuning-add-phase2
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-05-12T13:49:20.731Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: PORT LOCK — Dev server MUST run on port 5176. Running on any other port silently breaks all car/mod data (localStorage is origin-scoped to `http://localhost:5176`). The app will show "Unknown" car name and empty build sheet with zero errors.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong dev server port | Vite auto-increments port when 5176 is occupied. On a new origin, localStorage `gdim_chosen_car_id` is missing — all queries return empty silently | blocking | Always kill whatever holds 5176 first: `lsof -ti :5176 \| xargs kill`. Then: `npx vite --port 5176 --host` |
| Read MASTER_ARCHITECTURE.md before any code | This file is the single source of truth and supersedes all other docs. Writing code without reading it first causes schema mismatches | advisory | Read `MASTER_ARCHITECTURE.md` in full before any implementation |

<current_state>
TuningAddPage has been fully rewritten into a 3-step animated flow (complete, TypeScript-clean). The outstanding bug is Step 2 ("Part type picker") returns "No part types found" for every category. The `part_types` table in Supabase is either empty, has `is_active = false` on all rows, or has category values that don't match the TUNING_CATEGORIES ids.
</current_state>

<completed_work>

- Task 1: Rewrote TuningAddPage.tsx into 3-step animated flow
  - Step 1: Category picker — pixel-identical to Phase 1 (same grid, icons, TILE_SHADOW)
  - Step 2: Part type picker — queries part_types WHERE category = X AND is_active = true
  - Step 3: Full form — Title, Brand, Part Number, Date Installed, Parts Cost ($), Labor Cost ($), Installed By (Self/Shop segmented toggle), Notes (uses notes_placeholder), Photos
  - Full Specs toggle — grouped by group_label; basic specs default visible; advanced behind "+ Advanced Specs" accordion
  - All 6 input types: text, number (unit inline), select, multiselect (checkboxes), boolean (toggle), date
  - Save: INSERT jobs → INSERT job_specs (non-empty only, multiselect joined with ,) → compress+EXIF strip via browser-image-compression → upload to job-photos bucket → INSERT job_photos → navigate('/tuning')
  - Animation: 300vw strip translateX — 280ms ease-in-out, direction inherent from step index
  - TypeScript clean
</completed_work>

<remaining_work>

- Task 2: Fix "No part types found" in Step 2
  - Diagnose: `SELECT id, name, category, is_active FROM part_types LIMIT 20` in Supabase SQL editor
  - If empty → seed data from migrations 024/026 not applied to live DB
  - If is_active = false → `UPDATE part_types SET is_active = true`
  - If category mismatch → verify values match part_categories.name (FK constraint from migration 025)
  - After fix: test full 3-step flow end-to-end → verify job saves → appears in build sheet
</remaining_work>

<decisions_made>

- 3-step animated slide via 300vw CSS strip (translateX) — no extra direction state needed
- boolean spec 'false' not inserted into job_specs — toggle-off means no data to store
- EXIF stripping mandatory — browser-image-compression with exifOrientation:-1
- Photo upload failure non-fatal — job is already saved, catch silently
- Dev server must be port 5176 — localStorage is origin-scoped
</decisions_made>

<blockers>

- **part_types table issue**: Step 2 returns "No part types found" for all categories. Diagnose in Supabase SQL editor first.
</blockers>

## Required Reading (in order)
1. `MASTER_ARCHITECTURE.md` — single source of truth; read in full before writing any code
2. `supabase/migrations/024_part_spec_system.sql` — part_types + spec_templates schema and seed data
3. `supabase/migrations/026_part_spec_domain.sql` — additional part_types seeds and notes_placeholder column

## Infrastructure State
- Dev server: `npx vite --port 5176 --host` — MUST be 5176, kill anything holding that port first
- Machine network IP: 192.168.3.4
- Supabase project: uxqoernfrtgclpneirvc
- TypeScript: clean
- browser-image-compression: installed (package.json updated)

<context>
Phase 2 of TuningAddPage is largely done — the 3-step UI is built, animated, wired up, and TypeScript-clean. The only gap is a data issue: the part_types table appears empty or misconfigured in the live Supabase project, so Step 2 shows "No part types found" for every category. The code is correct; the migrations may not have been fully applied to the live DB.
</context>

<next_action>
Start with: Open Supabase SQL editor for project uxqoernfrtgclpneirvc → run `SELECT id, name, category, is_active FROM part_types LIMIT 20` → diagnose based on result → fix → verify Step 2 shows part types → test full 3-step flow end-to-end.
</next_action>
