---
context: default
phase: scaffold-to-auth
task: 9
total_tasks: 24
status: in_progress
last_updated: 2026-05-03T04:41:57Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> No blocking constraints discovered through failure this session. Section retained for future use.

<current_state>
G-Dimension PWA at ~/Desktop/g-dimension. Build Order (MASTER_ARCHITECTURE.md Part 26) Steps 1–5 are done. Architecture was upgraded to v3.0 this session. The existing code (App.tsx routes, tokens/index.ts) was written against v2.1 and has NOT yet been updated to reflect v3.0 changes. There are uncommitted files. The next immediate task is: commit outstanding files, then update App.tsx and tokens to match v3.0 before starting any new screens.
</current_state>

<completed_work>

- Step 1 DONE: Vite + React 18 + TypeScript + React Router v6 + Tailwind CSS scaffolded (commit fdbc691)
- Step 2 DONE: Design tokens in src/tokens/index.ts — Parts 3–8 from v2.1 (needs v3 timeline tokens added)
- Step 3 DONE: Base component stubs — Header, ConcretePanelInput, DestinationNode, SkeuomorphicIconWrapper
- Step 4 DONE: Supabase setup done externally by user — migrations 001–023, auth configured, storage buckets created
- Step 5 PARTIAL: Landing page built and deployed. Login/Signup screens NOT built yet.
- Vercel routing fixed: marketing page at /, React SPA at all other routes (vercel.json routes config)
- GitHub MCP installed in Claude Code config
- MASTER_ARCHITECTURE.md renamed from MASTER_ARCHITECTURE_V3.md (not yet committed)
- supabase/ folder created by user with all migration files (not yet committed to git)
</completed_work>

<remaining_work>

IMMEDIATE (fix before writing any new code):
- Commit MASTER_ARCHITECTURE.md + supabase/ folder
- Fix App.tsx Tuning routes — v3 changed these completely:
  OLD: /tuning/:category, /tuning/:category/new
  NEW: /tuning/build-sheet, /tuning/blueprint, /tuning/parts-bin, /tuning/add
- Fix App.tsx: /entries/:entryId → /timeline/entry/:entryId
- Fix App.tsx: /maintenance/:serviceId → /maintenance/:sessionId
- Add Timeline color tokens to src/tokens/index.ts (v3 Part 3 added --timeline-bg, --timeline-card, --timeline-text, --timeline-muted, --timeline-year, --timeline-rule, --timeline-chevron, --timeline-mod, --timeline-service, --timeline-detail)
- Add RADIUS_TIMELINE_CARD = 4 to tokens (v3 Part 8 — only exception to no-radius rule)
- Add timeline card shadow to tokens (v3 Part 6: box-shadow: 0 4px 16px rgba(0,0,0,0.10), 0 1px 4px rgba(0,0,0,0.06))
- Add timelineEntryReveal keyframe to tokens (v3 Part 7)
- Create src/utils/unitConversion.ts — mi↔km, hp↔ps↔kw, lbft↔nm (Part 16)
- Create src/assets/icons/tuning-dashboard/ folder placeholder

NEEDS USER INPUT:
- Supabase DB connection string → configure PostgreSQL MCP
- GitHub PAT → authenticate GitHub MCP (github.com/settings/tokens, repo scope)

BUILD ORDER REMAINING (Part 26 Steps 5–24):
- Step 5: Login (/login) and Signup (/signup) screens wired to Supabase auth
- Step 6: Home map screen — 5 destination nodes, full header, amber halo
- Step 7: Garage hero screen
- Step 8: Garage dashboard 3×2 grid
- Step 9: My Cars carousel
- Step 10: Add Car form with variant autocomplete
- Step 11: Onboarding coachmarks
- Step 12: Tuning destination (Build Sheet, Blueprint, Parts Bin, Add Mod)
- Step 13: Maintenance destination
- Step 14: Timeline destination
- Steps 15–24: Photos, Snapshot, Profile/Settings, Public Profile, Error logging, etc.
</remaining_work>

<decisions_made>

- vercel.json uses `routes` (not `rewrites`) — Vite framework preset overrides rewrites; routes takes full control
- marketing/index.html deleted; public/marketing.html is the single source (copied to dist/ by Vite)
- All units stored in base (miles, hp, lb-ft), converted at display time only — no data migration ever needed
- users.id references auth.users.id directly — not a surrogate key; trigger auto-creates public.users row
- Edge Function (not pg_cron) for nightly purge — pg_cron can't access Storage API
- car-documents is a PRIVATE bucket — VINs, insurance, registration must never be public
- Public build sheet view excludes cost columns — enthusiast culture shows parts, not spend
- Build Investment private by default, toggled per car via show_investment_publicly
- vehicle_variants layer added — S14 Kouki and S14 Zenki are meaningfully different cars
- 100+ search aliases — "Evo", "S14", "240SX", "AE86", "hachi roku" all resolve correctly
</decisions_made>

<blockers>

- MASTER_ARCHITECTURE.md and supabase/ not committed to git yet — do this first
- PostgreSQL MCP: installed package but needs SUPABASE_DB_URL env var configured
- GitHub MCP: added to config but needs PAT authentication (user to provide token)
- Supabase migrations status unverified — run checklist in MASTER_ARCHITECTURE.md Part 30
- import_nhtsa.js not yet run — vehicle autocomplete won't work until this runs
</blockers>

## Required Reading (in order)
1. `MASTER_ARCHITECTURE.md` — read in FULL before writing any code. v3.0 is a significant upgrade from v2.1.
2. `supabase/migrations/SUMMARY.md` — all schema decisions from 3 Supabase sessions
3. `.planning/HANDOFF.json` — structured state for machine parsing

## Infrastructure State
- **gdimension.app**: Live — marketing page at /, React landing page at /login+/signup etc.
- **Vercel**: Connected to GitHub prokaiwa/g-dimension-app, auto-deploys on push to main
- **Supabase**: Project set up at hi@gdimension.app — migrations reportedly run but unverified
- **GitHub MCP**: Added to Claude Code config, needs PAT to authenticate
- **PostgreSQL MCP**: Package not installed (npm error) — retry with: `npm install -g @modelcontextprotocol/server-postgres`
- **Dev server**: Not running — start with `npm run dev` in ~/Desktop/g-dimension

## Key v3.0 Changes From v2.1 (that affect existing code)

| What changed | Old | New |
|---|---|---|
| Tuning routes | /tuning/:category | /tuning/build-sheet, /tuning/blueprint, /tuning/parts-bin, /tuning/add |
| Timeline entry route | /entries/:entryId | /timeline/entry/:entryId |
| Maintenance detail route | /maintenance/:serviceId | /maintenance/:sessionId |
| Timeline has NO header | was assumed to have minimal header | floating amber ‹ only |
| Photos has NO header | was assumed to have minimal header | floating amber ‹ only |
| New color tokens | none | full timeline palette (9 new tokens) |
| Border-radius exception | pills, sheet, avatars, dots, badges | + timeline cards: 4px |
| Data model | modifications table | sessions + jobs tables |
| Unit system | torque_unit per car | all units per user, stored in base |

<context>
App is scaffolded and live. The Supabase database is set up. Architecture v3.0 is the bible. The most important thing before writing new screens is getting the existing code (App.tsx routes + tokens) in sync with v3.0 — otherwise every screen built on top will have the wrong foundation. After that, Step 5 (Login + Signup with real Supabase auth) is the next meaningful deliverable.
</context>

<next_action>
Start with: `git add -A && git commit -m "chore: add supabase migrations folder and rename architecture to v3"` then fix App.tsx routes and add timeline tokens per MASTER_ARCHITECTURE.md v3.0 Parts 3, 7, 8, and 10.
</next_action>
