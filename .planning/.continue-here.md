---
context: default
phase: build-order
task: 28
total_tasks: 30
status: paused
last_updated: 2026-05-08T03:48:13.898Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Task 27 (Snapshot screen) is complete and committed (9fe33b5). Working tree is clean.
Next task is 28 — Tuning screen at /tuning.

URGENT FIRST: Fix pre-existing Vercel build errors before any new push.
Errors in HomePage.tsx (FONT_TITLE, SHADOW_ICON_FOCAL, SHADOW_ICON_STANDARD, SHADOW_GROUND, 'entered' var)
and GaragePage.tsx (GRADIENT_APP_BG, COLOR_CAVITY_BG, SPACE_MD) — all unused imports (TS6133).
NOT caused by today's work. Fix, run `npm run build`, commit, push before starting task 28.
</current_state>

<completed_work>

Completed this session:
- Pushed 9 previously uncommitted commits to GitHub
- Task 27: Snapshot screen — full redesign as practical "show your mechanic" handoff card
  - Sections: Vehicle (VIN, plate, engine, tires, oil, battery, mileage) → Service → Important Dates (registration + insurance renewal, own section) → Contacts (roadside, mechanic, autobody, dealership)
  - Hero: photo box left (150×110, background-image div to bypass Tailwind Preflight), Hanken Grotesk bold italic -0.1em car name right, color shown below make
  - Light grey palette (#e8e8e6 bg, #1a1a1c text)
  - HP/torque/drivetrain/transmission removed from Snapshot — saved for Build Sheet
  - Details overlay in My Cars: added Vehicle Specs section (engine, forced induction, HP, torque, transmission, drivetrain, chassis code, VIN, plate, oil type, tire size, battery model)
  - Choose button: saves gdim_chosen_car_id to localStorage → navigates to /garage
  - Fixed Tailwind Preflight img override using background-image div
  - Fixed TypeScript cast errors (as unknown as SnapshotCar, WebkitAppearance as any)
  - Committed and pushed: 9fe33b5
</completed_work>

<remaining_work>

- Task 25b: DB cleanup — remove Honda motorcycles, consolidate Silvia/Skyline (non-blocking, human SQL)
- Task 28: Tuning screen — /tuning category browsing (NEXT after Vercel fix)
- Task 29: Timeline screen — /timeline chronological build narrative
- Task 30: Maintenance screen — /maintenance service logging
</remaining_work>

<decisions_made>

- Snapshot = practical handoff card (not spec sheet). Build Sheet (/tuning/build-sheet, future) = enthusiast full specs
- Snapshot sections: Vehicle → Service → Important Dates (own section) → Contacts
- Details overlay expanded with Vehicle Specs section (not a separate edit screen)
- Choose navigates to /garage after setting chosen car in localStorage
- photo_y_offset (0-100, default 50) drives vertical crop for car photo in Snapshot hero
- background-image on div (not img tag) bypasses Tailwind Preflight height:auto reset on all img elements
</decisions_made>

<blockers>

- Vercel build failing: unused imports in HomePage.tsx and GaragePage.tsx (TS6133). Pre-existing, not from today's work. Must fix before next push.
</blockers>

## Required Reading (in order)
1. `MASTER_ARCHITECTURE.md` — ALWAYS read before writing any code. Single source of truth. Build order in Part 21.
2. `src/tokens/index.ts` — all design tokens
3. `src/pages/GarageCarsPage.tsx` — reference for header pattern, GarageBg, shared styles

## Infrastructure State
- Dev server: `npx vite --port 5176 --host` (local network: 192.168.3.9:5176)
- Supabase project: uxqoernfrtgclpneirvc
- All code committed and pushed (main, 9fe33b5)
- Vercel: FAILING — pre-existing TS6133 unused import errors. Fix before pushing.

<context>
Snapshot screen is done and user is happy. The "show your mechanic" framing was the right call.
Service/Contacts stubs fill automatically when Reminders and Contacts screens are built.

Key architecture decision: Snapshot ≠ Build Sheet.
- Snapshot = for anyone (mechanic, spouse, dealer)
- Build Sheet (at /tuning/build-sheet, already in roadmap) = for enthusiasts

Next is Tuning screen (/tuning). Read MASTER_ARCHITECTURE.md Part 11 carefully before starting.
</context>

<next_action>
1. Fix Vercel build errors: remove unused imports from src/pages/HomePage.tsx and src/pages/GaragePage.tsx, run `npm run build` to confirm clean, commit and push.
2. Begin task 28: Tuning screen at /tuning. Read MASTER_ARCHITECTURE.md Part 11 first.
</next_action>
