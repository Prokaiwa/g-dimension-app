---
context: default
phase: build-order
task: 29
total_tasks: 30
status: paused
last_updated: 2026-05-08T12:48:19.425Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

_No blocking constraints identified this session._

<current_state>
Task 28c (TuningBuildSheetPage + TuningAddPage) is complete and pushed (ca2fea3).
Working tree is clean. All code passes tsc --noEmit.

Next task is 28d — TuningBlueprintPage. Same pattern as BuildSheet but
queries status='planned' (wishlist mods). TUNING_CATEGORIES is already
exported from TuningBuildSheetPage for reuse.
</current_state>

<completed_work>

This session:
- Task 28a: TuningPage hero — committed (31e3cc9)
- Task 28b: iOS press feedback — onPointerDown/onPointerUp replacing CSS :active on
  TuningPage, GaragePage, HomePage, GarageCarsPage. Stagger delay 400→80ms so icons
  are interactive immediately. Committed (fb890fd, fbb2359, 93eaca1, 6d10ffb)
- Task 28c: TuningBuildSheetPage + TuningAddPage — committed (ca2fea3)
  - Empty state: 3×5 category grid + phosphorous green #39ff14 "+ Add Mods" CTA
  - Filled state: spec list grouped by category with floating FAB
  - Add flow: two-step (category picker → form). Black bg + blue from bottom
    + burgundy from left gradient. Fields: Part Name, Brand, Category (display),
    Date Installed, Notes. "Add DIY Steps" stub (future phase).
  - TUNING_CATEGORIES exported for Blueprint/PartsPage reuse
</completed_work>

<remaining_work>

- Task 28d: TuningBlueprintPage — wishlist mods. Same pattern as BuildSheet,
  query status='planned'. NEXT.
- Task 28e: TuningPartsPage — Parts Bin. status='purchased'.
- Task 25b: DB cleanup (human SQL, non-blocking)
- Task 29: Timeline screen — /timeline
- Task 30: Maintenance screen — /maintenance
</remaining_work>

<decisions_made>

- iOS press: onPointerDown state (not CSS :active) — fires instantly on iOS,
  no Safari delay. Inner wrapper div carries transform so it doesn't fight
  the button's fade-in animation.
- STAGGER_BASE_MS 400→80 — 400ms was too long, icons appeared non-interactive.
- TUNING_CATEGORIES exported from TuningBuildSheetPage — shared by all tuning
  sub-pages (Blueprint, PartsPage, AddPage).
- TuningAddPage two-step flow: category picker → form. Pre-selects via ?category=
  query param. Same page handles adds from any tuning sub-section.
- Phosphorous green #39ff14 with glow for Add Mods CTA (user-specified).
- "Add DIY Steps" is a future phase — needs mod_steps table, image upload,
  step editor. Stub button reserved on the form.
</decisions_made>

<blockers>
None blocking. Non-blocking human SQL tasks pending (Datsun duplicate,
Estima SQL, Honda motorcycle cleanup).
</blockers>

## Required Reading (in order)
1. `src/pages/TuningBuildSheetPage.tsx` — exports TUNING_CATEGORIES; reference for
   Blueprint/PartsPage pattern and header style
2. `src/pages/TuningAddPage.tsx` — add flow; can be extended with ?status= param
3. `MASTER_ARCHITECTURE.md` Part 11 — Blueprint and Parts Bin design spec

## Infrastructure State
- Dev server: `npx vite --port 5176 --host` (local: 192.168.3.9:5176)
- Supabase project: uxqoernfrtgclpneirvc
- Git: clean, all pushed to main (ca2fea3)
- Vercel: passing (tsc clean)

<context>
BuildSheet and AddPage are done and working. Blueprint (28d) is nearly
identical to BuildSheet — just change the query status from 'installed'
to 'planned' and update the empty-state copy. The add flow at /tuning/add
already works; Blueprint can navigate to it with ?category=X.

The iOS press feedback improvements touched 4 screens and are fully
deployed. The stagger delay fix makes all dashboard screens feel instant.
</context>

<next_action>
Build TuningBlueprintPage (28d):
1. Read TuningBuildSheetPage.tsx for the full pattern
2. Import TUNING_CATEGORIES from TuningBuildSheetPage
3. Query: status='planned' AND type='modification'
4. Same empty state (grid + green CTA) and filled state (grouped list)
5. Empty-state button label: something like "Plan a Mod"
</next_action>
